# 개발 환경 네트워크 설정
# 이 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다

# Next.js 개발 서버 호스트 설정 (모든 네트워크 인터페이스에서 접근 허용)
HOSTNAME=0.0.0.0
PORT=3000

# 네트워크 접근 허용 설정
NEXT_PUBLIC_ALLOW_NETWORK_ACCESS=true

# 개발 모드 최적화 설정
NEXT_TELEMETRY_DISABLED=1
NODE_ENV=development

# Database (개발용)
DATABASE_URL="postgresql://username:password@localhost:5432/ai_gallery_dev"

# NextAuth 설정 (개발용)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-development-secret-key

# AWS S3 설정 (개발용)
AWS_ACCESS_KEY_ID=your-dev-access-key
AWS_SECRET_ACCESS_KEY=your-dev-secret-key
AWS_REGION=us-west-2
AWS_S3_BUCKET=ai-gallery-dev-bucket